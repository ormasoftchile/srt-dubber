# Orchestration: Butch — Slice 6B

**Agent:** Butch  
**Timestamp:** 2026-04-30T13:56:24Z  
**Task:** AssembleFlow TUI wiring + Project cleanup  
**Status:** ✓ COMPLETE

## Deliverables

- `assemble_screen.cpp` wired to AssembleFlow machine
- Command queue pattern: thread → machine posts via queue
- Removed transient Project fields: `assemble_log`, `complete`, `output_path`
- **Tests:** All tests passing; no regressions
- LOC reduction in assemble_screen.cpp

## Rationale

Eliminates direct state mutation in TUI. AssembleFlow machine now owns all assembly logic. Transient fields were read/written only in assemble_screen.cpp, safe to remove (validated by grep).

## Risk Mitigation

- Command queue decouples thread from machine
- Effects dispatcher centralizes all side effects
- No direct Project mutation from TUI layer

## Design Tradeoffs

- Assembly thread remains blocking (ffmpeg spawned as subprocess)
- `stop_token` provided but is best-effort only (ffmpeg subprocess may continue)
- These are architectural constraints, not bugs

## Next Phase

Slice 8: multi-screen harness drives all machines (nav + record + review + assemble) via JSON protocol.
