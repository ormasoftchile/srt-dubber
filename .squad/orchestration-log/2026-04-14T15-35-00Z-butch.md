# Orchestration Log Entry

### 2026-04-14T15:35:00Z — --device N flag for input device selection

| Field | Value |
|-------|-------|
| **Agent routed** | Butch (Audio & Backend Dev) |
| **Why chosen** | Feature is audio-layer only: device selection by index requires changes to `AudioRecorder` constructor and `start()`, plus a thin pass-through in `App` and CLI parsing in `main.cpp` — all within Butch's ownership of `src/audio/` and miniaudio integration |
| **Mode** | `background` |
| **Why this mode** | No hard data dependencies on other agents; all changes confined to `src/audio/recorder.hpp`, `src/audio/recorder.cpp`, `src/tui/app.hpp`, `src/tui/app.cpp`, and `main.cpp` |
| **Files authorized to read** | `src/audio/recorder.cpp`, `src/audio/recorder.hpp`, `src/tui/app.hpp`, `src/tui/app.cpp`, `main.cpp`, `vendor/miniaudio.h`, `.squad/agents/butch/history.md`, `.squad/decisions.md` |
| **File(s) agent must produce** | `src/audio/recorder.hpp` (modified), `src/audio/recorder.cpp` (modified), `src/tui/app.hpp` (modified), `src/tui/app.cpp` (modified), `main.cpp` (modified), `.squad/agents/butch/history.md` (updated) |
| **Outcome** | Completed — `--device N` flag added; `AudioRecorder` constructor accepts `device_index`; selects capture device by enumeration index at `start()`; falls back to default with warning if out of range; `App` passes index through; `main.cpp` parses flag and prints `--list-devices` hint. Build clean. |
